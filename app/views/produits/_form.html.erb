<%= form_for(@produit) do |f| %>
  <% if @produit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@produit.errors.count, "error") %> prohibited this produit from being saved:</h2>

      <ul>
      <% @produit.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :prix %><br>
    <%= f.text_field :prix %>
  </div>
  <div class="field">
    <%= f.label :catégorie %><br>
    <%= f.collection_select :category_id, @categories, :id, :nom %>
  </div>
  <div class="field">
    <%= f.label :vendeur %><br>
    <%= f.collection_select :vendeur_id, @vendeurs, :id, :pseudo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


